[build]
buildCommand = "cd server && npm install && npx prisma@6.7.0 generate"

[deploy]
startCommand = "cd server && npx prisma@6.7.0 db push --skip-generate && node src/index.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
