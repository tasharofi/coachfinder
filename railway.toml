[build]
buildCommand = "cd server && npm install && npx prisma generate"

[deploy]
startCommand = "cd server && npx prisma migrate deploy && node src/index.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
