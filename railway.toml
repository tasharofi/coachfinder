[build]
buildCommand = "cd server && npm install && npx prisma generate"

[deploy]
startCommand = "cd server && npx prisma db push --skip-generate && node src/index.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
