[build]
buildCommand = "npm install && npx prisma@6.7.0 generate"

[deploy]
startCommand = "npx prisma@6.7.0 db push --skip-generate --accept-data-loss && node src/index.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
